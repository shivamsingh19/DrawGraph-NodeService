var moment = require('moment');

var getRange = function (data) {
    return {'suraj': 1};
    // var startDate = moment(data.startDate).utc().format();
    // var endDate = moment(data.endDate).utc().format();
    // var dateRanges = [
    //   {
    //     "date" : startDate,
    //     "value" : getRandomInt(1,1000)
    //   }
    // ];
    // while(endDate.isAfter(startDate)) {
    //   startDate = startDate.add(1, 'seconds');
    //   dateRanges.push(
    //       {
    //           "date" : startDate,
    //           "value" : getRandomInt(1,1000)
    //       }
    //     );
    // }
    // return dateRanges;
};

// function getRandomInt(min, max) {
//   min = Math.ceil(min);
//   max = Math.floor(max);
//   return Math.floor(Math.random() * (max - min)) + min;
// }

